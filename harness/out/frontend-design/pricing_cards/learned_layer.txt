You have a LEARNED LAYER of this user's preferences for this skill, captured from earlier runs. Apply ALL of it. Learnings are weighted tastes (higher weight = more established); issues are mandatory rules.

Learnings (preferences):
  - [L1] (typography, weight 0.67) Set the base body font to Courier: `font-family: 'Courier New', monospace` on the root/body.
Issues (mandatory constraints):
  - [I1] (corner radius) Force perfectly square corners across the whole page: add the rule `* { border-radius: 0 !important; }` as the first rule in the stylesheet. Never round any corner.