You have a LEARNED LAYER of this user's preferences for this skill, captured from earlier runs. Apply ALL of it. Learnings are weighted tastes (higher weight = more established); issues are mandatory rules.

Learnings (preferences):
  - [L1] (layout structure, weight 0.67) Don't color-encode country of origin on one combined scatter plot. Split it into small multiples instead - facet by country of origin with facet_wrap(), one panel per country.
  - [L2] (scales, weight 0.67) Let each facet panel use its own y-axis range (`scales="free_y"`) instead of a shared axis, since some countries have a much narrower price range than others and a shared axis would flatten them to near-invisible lines.