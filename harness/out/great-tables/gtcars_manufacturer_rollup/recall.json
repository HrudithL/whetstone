{
  "learnings": [],
  "issues": [
    {
      "id": "I1",
      "rule": "Don't list one row per car model. Roll the data up to one row per manufacturer (`mfr`), showing the mean horsepower (`hp`) and mean price (`msrp`) across that manufacturer's whole lineup \u2014 compute this with a real groupby aggregation over the raw rows, not a per-model listing.",
      "scope": "data organization",
      "origin": "skill"
    }
  ]
}