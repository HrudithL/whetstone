"""Whetstone showcase harness (M3) — internal, command-only tooling.

Not part of the distributed ``whetstone`` package and never imported by the MCP server. It generates
the before/after artifacts and metrics the Quarto site (``docs/``) reads from ``harness/out/``. See
``harness/README.md``.
"""
